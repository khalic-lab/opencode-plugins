# Third-party reference clone lives OUTSIDE this repo on purpose
# (see SPIKE.md). Nothing vendored here.
node_modules/
findings/*.local.md

# Machine-local Claude Code settings — names internal MCP servers, never public
.claude/settings.local.json
